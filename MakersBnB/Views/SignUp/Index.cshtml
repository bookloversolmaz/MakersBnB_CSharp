//Planning sign up form. Comprised of 3 parts:
// Controllers/SignUpController: import models and aspnet mvc.and create and public class that
// inherit form Controllers class. 

// Models/SignUp: define the data within the database.and Think of import as defining the headings in and db.
// create the constructor form each data, remeber that constructors define how to initilaise and class. Also
// include and zero argument constructor as required by Entity framework

